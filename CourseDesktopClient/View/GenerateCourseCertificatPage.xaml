<UserControl x:Class="CourseDesktopClient.View.GenerateCourseCertificatPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CourseDesktopClient.View"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:converter="clr-namespace:CourseDesktopClient.UI.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="932.425" d:DesignWidth="1088" >
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadedPageCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <UserControl.Resources>
        <converter:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
    </UserControl.Resources>

    <Grid Background="{StaticResource OffWhiteBrush}">
        <Border Background="{StaticResource PureWhiteBrush}"
                Width="550"
                Padding="40"
                CornerRadius="24"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">

            <Border.Effect>
                <DropShadowEffect BlurRadius="20" 
                                  ShadowDepth="4" 
                                  Opacity="0.12" 
                                  Direction="270"
                                  Color="#000000"/>
            </Border.Effect>

            <StackPanel>

                <!-- Заголовок -->
                <TextBlock Text="Получение сертификата"
                           Style="{StaticResource H1Style}"
                           Margin="0,0,0,8"
                           HorizontalAlignment="Center"/>

                <TextBlock Text="Заполните данные для генерации именного сертификата"
                           Style="{StaticResource SecondaryTextStyle}"
                           Margin="0,0,0,28"
                           HorizontalAlignment="Center"/>



                <!-- ФИО пользователя -->
                <TextBlock Text="Ваше ФИО"
                           Style="{StaticResource FieldLabelStyle}"
                           Margin="0,0,0,5"/>

                <TextBox Text="{Binding UserFullName, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource TextBoxWithIconAndHintStyle}"
                         converter:TextBoxExtensions.IconSource="/UI/Images/icons8-user-60.png"
                         Tag="Иванов Иван Иванович"
                         Height="45"
                         MaxLength="30"
                         FontSize="14"
                         Margin="0,0,0,15"/>
                <!-- Путь сохранения -->
                <TextBlock Text="Путь сохранения"
                           Style="{StaticResource FieldLabelStyle}"
                           Margin="0,0,0,5"/>

                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox Text="{Binding SavePath, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource TextBoxWithIconAndHintStyle}"
                             converter:TextBoxExtensions.IconSource="/UI/Images/icons8-folder-96.png"
                             Tag="Выберите папку для сохранения"
                             Height="45"
                             IsEnabled="False"
                             FontSize="14"
                             Grid.Column="0"/>

                    <Button Content="Обзор"
                            Command="{Binding BrowseFolderCommand}"
                            Style="{StaticResource BrowseButtonStyle}"
                            Grid.Column="1"
                            Margin="10,0,0,0"/>
                </Grid>


                <!-- Сообщение об ошибке -->
                <TextBlock Text="{Binding MisstakeText}" 
                        Visibility="{Binding VisibleMisstake}"
                        Style="{StaticResource ErrorTextStyle}" 
                        HorizontalAlignment="Center"
                        Margin="0,0,0,15"/>

                <!-- Кнопка скачивания -->
                <Button Content="Скачать сертификат"
                        Command="{Binding DownloadCertificateCommand}"
                        Style="{StaticResource DownloadButtonStyle}"
                        Height="48"
                        FontSize="15"
                        Margin="0,20,0,15"/>

            </StackPanel>
        </Border>
    </Grid>
</UserControl>