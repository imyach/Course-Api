<UserControl x:Class="CourseDesktopClient.View.VideoLogoPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CourseDesktopClient.View"
             xmlns:shader="clr-namespace:CourseDesktopClient.UI.Shaders"
              xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{StaticResource OffWhiteBrush}">
        <!-- Основной контейнер с максимальной шириной -->
        <Border Background="{StaticResource PureWhiteBrush}"
        Padding="40"
        CornerRadius="20"
        HorizontalAlignment="Center"
        VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" 
                          ShadowDepth="3" 
                          Opacity="0.15" 
                          Direction="270"
                          Color="#000000"/>
            </Border.Effect>
            <MediaElement x:Name="VideoPlayer"
                  UnloadedBehavior="Stop"
                  LoadedBehavior="Play"
                  Width="870"
                  Source="{Binding SourseVideo}"
                  Height="auto">
                <MediaElement.Clip>
                    <RectangleGeometry Rect="2,2,870,486"/>
                </MediaElement.Clip>
                <MediaElement.Effect>
                    <shader:DeeperColorEffect/>
                </MediaElement.Effect>
            </MediaElement>
        </Border>
    </Grid>
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="MediaEnded" SourceObject="{Binding ElementName=VideoPlayer}">
            <i:InvokeCommandAction Command="{Binding EndVideoCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
</UserControl>
