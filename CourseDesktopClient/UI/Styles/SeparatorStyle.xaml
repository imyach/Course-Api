<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Стиль для разделителя меню -->
    <Style x:Key="MenuSeparatorStyle" TargetType="Separator">
        <Setter Property="Background" Value="{StaticResource LightGrayBrush}"/>
        <Setter Property="Height" Value="2"/>
        <Setter Property="Margin" Value="5,2"/>
    </Style>

    <Style x:Key="CardSeparatorStyle" TargetType="Rectangle">
        <Setter Property="Height" Value="1"/>
        <Setter Property="Fill" Value="{StaticResource MediumGrayBrush}"/>
        <Setter Property="Margin" Value="0,8,0,8"/>
    </Style>
    <!-- ========== СТИЛЬ ДЛЯ РАЗДЕЛИТЕЛЯ СЕКЦИЙ ========== -->
    <Style x:Key="SectionSeparatorStyle" TargetType="Separator">
        <Setter Property="Background" Value="{StaticResource BorderGrayBrush}"/>
        <Setter Property="Height" Value="1"/>
        <Setter Property="Margin" Value="0,10,0,10"/>
        <Setter Property="Opacity" Value="0.3"/>
    </Style>
</ResourceDictionary>