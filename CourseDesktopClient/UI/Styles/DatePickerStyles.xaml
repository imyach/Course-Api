<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:primitives="clr-namespace:System.Windows.Controls.Primitives;assembly=PresentationFramework">

    <!-- Стиль для DatePicker -->
    <Style x:Key="DatePickerStyle" TargetType="DatePicker">
        <Setter Property="Height" Value="45"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="Background" Value="{StaticResource PureWhiteBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderGrayBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DatePicker">
                    <Border x:Name="MainBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8"
                            Padding="12,0">

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Текстовое поле -->
                            <DatePickerTextBox x:Name="TextBox"
                                               Grid.Column="0"
                                               Text="{TemplateBinding Text}"
                                               IsReadOnly="True"
                                               Background="Transparent"
                                               BorderThickness="0"
                                               Cursor="Arrow"
                                               FontSize="{TemplateBinding FontSize}"
                                               Foreground="{TemplateBinding Foreground}"
                                               VerticalContentAlignment="Center"/>

                            <!-- Кнопка открытия календаря -->
                            <Button x:Name="PART_Button"
                                    Grid.Column="1"
                                    Width="30"
                                    Height="30"
                                    Margin="5,0,0,0"
                                    VerticalAlignment="Center"
                                    Cursor="Hand"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Foreground="{StaticResource TextSecondaryBrush}">

                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="Transparent"
                                                CornerRadius="8">
                                            <TextBlock x:Name="icon"
                                                       Text="📅"
                                                       FontSize="14"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"
                                                       Foreground="{TemplateBinding Foreground}"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="icon" Property="Foreground" Value="{StaticResource PrimaryGreenBrush}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>

                            <!-- Всплывающий календарь -->
                            <Popup x:Name="PART_Popup"
                                   IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   StaysOpen="False"
                                   Placement="Bottom"
                                   AllowsTransparency="True">
                                <Border Background="{StaticResource PureWhiteBrush}"
                                        BorderBrush="{StaticResource BorderGrayBrush}"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Padding="5">
                                    <Calendar x:Name="PART_Calendar"
                                              Style="{TemplateBinding CalendarStyle}"
                                              SelectedDate="{Binding SelectedDate, RelativeSource={RelativeSource TemplatedParent}}"
                                              DisplayDate="{Binding DisplayDate, RelativeSource={RelativeSource TemplatedParent}}"/>
                                </Border>
                            </Popup>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="MainBorder" Property="BorderBrush" Value="{StaticResource PrimaryGreenBrush}"/>
                            <Setter TargetName="MainBorder" Property="BorderThickness" Value="2"/>
                        </Trigger>
                        <Trigger Property="IsDropDownOpen" Value="True">
                            <Setter TargetName="PART_Button" Property="Foreground" Value="{StaticResource PrimaryGreenBrush}"/>
                        </Trigger>

                        <!-- Триггер для отключенного состояния -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="MainBorder" Property="Background" Value="{StaticResource LightGrayBrush}"/>
                            <Setter TargetName="MainBorder" Property="BorderBrush" Value="{StaticResource LightGrayBrush}"/>
                            <Setter TargetName="TextBox" Property="Foreground" Value="{StaticResource DarkGrayBrush}"/>
                            <Setter TargetName="PART_Button" Property="Foreground" Value="{StaticResource DarkGrayBrush}"/>
                            <Setter TargetName="PART_Button" Property="Cursor" Value="Arrow"/>
                            <Setter Property="Foreground" Value="{StaticResource DarkGrayBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>