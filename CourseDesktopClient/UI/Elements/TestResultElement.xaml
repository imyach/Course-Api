<UserControl x:Class="CourseDesktopClient.UI.Elements.TestResultElement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CourseDesktopClient.UI.Elements"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Background="LightGray" 
            BorderBrush="#E0E0E0" 
            BorderThickness="0 0 0 1"
            Padding="14"
            CornerRadius="20"
            Height="Auto"
            MaxHeight="130"
            Margin="20">

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Vertical" Grid.Row="0">
                <TextBlock 
                       Text="{Binding TestTitle}" 
                       FontSize="18"
                       FontWeight="Bold"
                       Foreground="#333"
                       Margin="0,0,0,8"/>
                <TextBlock
Text="{Binding CompletedAt, StringFormat='Завершен: {0:dd.MM.yyyy}'}" 
Foreground="#666"
HorizontalAlignment="Right">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CompletedAt}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>

            <StackPanel 
                        Orientation="Horizontal"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        Grid.Column="1">

                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" 
                           Margin=" 0 0 8 0" Foreground="#666"
                            TextWrapping="WrapWithOverflow"
                            TextTrimming="CharacterEllipsis"
                            MaxHeight="89" 
                            Height="Auto">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Text" Value="Статус: Не завершен"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsPassed}" Value="True">
                                    <Setter Property="Text" Value="Статус: Завершен"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <Button Content="Пройти"
                        Command="{Binding DataContext.PassTestCommand, 
                                RelativeSource={RelativeSource 
                                AncestorType=UserControl, 
                                AncestorLevel=2}}"
                        Visibility="{Binding ButtonVisible}"
                        CommandParameter="{Binding}"
                        Padding="12,6"
                        Margin="0,0,8,0"/>

                <Button Content="Посмотреть результаты"
                    Command="{Binding DataContext.LookTestResults, 
                            RelativeSource={RelativeSource 
                            AncestorType=UserControl, 
                            AncestorLevel=2}}"
                    CommandParameter="{Binding}"
                    Padding="12,6"
                    Margin="0,0,8,0">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsPassed}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

            </StackPanel>
        </Grid>
    </Border>
</UserControl>
